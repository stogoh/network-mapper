export interface VersionDetectionOption {
    versionDetection?: boolean // -sV and -sR are the same
}

export interface ForceScanAllPortsOption {
    forceScanAllPorts?: boolean
}

export interface VersionIntensityOption {
    versionIntensity?: number
}

export interface VersionTraceOption {
    versionTrace?: boolean
}